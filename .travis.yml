language: java
jdk:
  - oraclejdk8

services:
  - docker

after_success:
  - cd client
  - docker build -t yadroclient .
  - cd ..
  - cd service
  - docker build -t yadroservice .
  - docker images

deploy:
  provider: script
  script: bash docker_push
  on:
    branch: master
